// Central theme export
import colors from './colors';
import typography from './typography';
import { spacing, padding, margin, borderRadius } from './spacing';
import shadows from './shadows';

export const theme = {
    colors,
    typography,
    spacing,
    padding,
    margin,
    borderRadius,
    shadows,
};

export default theme;
